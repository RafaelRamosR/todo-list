# Simple ToDo List 📋
Create a list of pending tasks to help manage the day to day.

## Getting Started 🚀
Open the `index.html` file located in the `./public` folder.

## Installation 🔧
1. Clone the repository: 
```bash
git clone git@github.com:RafaelRamosR/todo-list.git
```

2. Install the dependencies: 
```shell
npm install
```

3. Compile and transpire the code: 
```shell
gulp
```

## Features ✅
- Add the task name 
- Assign a priority to tasks 
- Select a date and time to complete the task 
- Validate the data entered 
- Generate a countdown until the task is completed 
- Delete any pending tasks 
- Show a message once the task is finished 
